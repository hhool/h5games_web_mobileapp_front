# h5games_web_mobileapp

轻量前端项目，用于展示 `mobileapp` 下的游戏聚合页面。基于 `vite`。

快速开始

```bash
cd h5games_web_mobileapp
npm install
npm run dev
```

构建与预览

```bash
npm run build
npm run preview
```

部署

将构建输出（`dist/`）部署到 Cloudflare Pages 或任何静态主机。部署后请确保 `games.json` 放置在站点根路径或修改 `src/main.js` 的请求路径。
